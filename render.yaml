services:
  - type: web
    name: compatiblah-backend
    env: go
    plan: free
    buildCommand: go mod download && cd backend && go build -o app
    startCommand: cd backend && ./app
    healthCheckPath: /health
    envVars:
      - key: GEMINI_API_KEY
        sync: false

