services:
  - type: web
    name: compatiblah-backend
    env: go
    plan: free
    rootDir: backend
    buildCommand: go mod download && go build -o app
    startCommand: ./app
    healthCheckPath: /health
    envVars:
      - key: GEMINI_API_KEY
        sync: false

